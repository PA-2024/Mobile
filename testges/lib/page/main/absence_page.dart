import 'package:flutter/material.dart';

class AbsencePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Absence Page'),
      ),
      body: Center(
        child: Text('Absence Page Content'),
      ),
    );
  }
}
